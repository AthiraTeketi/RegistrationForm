import React from 'react'
import ReactDOM from 'react-dom/client'
import Regform from './App.js'
// import Regform from './header'
const root = ReactDOM.createRoot(document.getElementById('root'))
root.render(
    <React.StrictMode>
    <Regform />
  </React.StrictMode>
)